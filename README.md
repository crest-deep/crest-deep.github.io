# crest-deep project page

nikola-powered version of the website


nikola version >8 is required, 
install using `pip install --pre`
use `nikola build` to generate output 
or `nikola auto` to serve locally

